info:
  name: Create a user
  type: http
  seq: 2

http:
  method: POST
  url: "{{dev}}/users"
  body:
    type: json
    data: |-
      {
        "name": "pepe",
        "lastName": "pancho",
        "userName": "pepe19a",
        "password": "12345",
        "email": "peape@gmail.com",
        "age": 30
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
